#include<iostream>
#include<cstring>

using namespace std;
long long t, n, h, r, xyz[1005][3], mark[1005], arr[1005][1005], s[1005], cnt, e[1005];

int main() {
    cin >> t;
    while (t--) {
        memset(mark, 0, sizeof(e));
        memset(arr, 0, sizeof(arr));
        cnt = 0;
        memset
    }


    return 0;
}