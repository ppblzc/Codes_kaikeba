#include<iostream>
#include<cstring>
using namespace std;

//全局变量无需手动初始化，全填充为0；
char s1[1005], s2[1005];
int num1[1005], num2[1005], sum[1005];

int main(){
    cin >> s1 >> s2;
    num1[0] = strlen(s1);
    num2[0] = strlen(s2);
    //s1[i]意为,当字符串没到结束时，就一直遍历下去；
    for (int i = 0; s1[i]; i++) {


    }
    
    return 0;
}